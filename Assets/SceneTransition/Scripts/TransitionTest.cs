using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

namespace WBTransition
{
    public class TransitionTest : MonoBehaviour
    {
        //[SerializeField] private SceneAsset newScene;
        //[SerializeField] private float closeTime;
        //[SerializeField] private float waitTime;
        //[SerializeField] private float openTime;
        //[SerializeField] private List<GameObject> decorations;

        //// Update is called once per frame
        //void Update()
        //{
        //    if (KeyInputAssist.Instance.GetKeyDown(KeyCode.Space))
        //    {
        //        TransitionManager.Instance.StartTransition(newScene, closeTime, waitTime
        //            , openTime,decorations);
        //    }
        //}
    }
}
